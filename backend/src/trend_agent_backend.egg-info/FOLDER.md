# backend/src/trend_agent_backend.egg-info/

## Purpose
This folder contains auto-generated package metadata produced by editable/install builds of the backend Python package. It captures resolved dependency declarations, package identity, and source manifest details used by packaging tooling. It exists separately from application code so build metadata can be regenerated without modifying core source modules.

## Files
| File | Purpose |
|---|---|
| `PKG-INFO` | Generated package metadata (name/version/python requirement/dependencies) for `trend-agent-backend` |
| `SOURCES.txt` | Build manifest listing files included in the package distribution |
| `requires.txt` | Flattened dependency requirement list (including optional dev extras) |
| `top_level.txt` | Declares top-level import package names shipped by this distribution |
| `dependency_links.txt` | Legacy dependency link metadata file (empty in current build) |

## Last Session Changes
**Session date:** 2026-05-06

**Changes made:**
- FOLDER.md created by `/create` — initial documentation pass.

**Reason:** Bootstrapping project documentation so that `/start` and `/end` can be used going forward.

**Outcome:** Complete — all files documented as of this date.

**Watch out for:** This documentation was generated from a static read of the code. If any files have changed since this was written, run `/end` after your next session to keep it current.

## Change Log
| Date | File(s) Changed | Summary |
|---|---|---|
| 2026-05-06 | Initial creation | FOLDER.md bootstrapped by `/create` |
